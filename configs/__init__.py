# Marker so the default deployment configs ship as package data in the wheel
# (see pyproject [tool.setuptools.package-data]). The CLI resolves defaults
# via importlib.resources; see mstar/cli/main.py:_resolve_config.
