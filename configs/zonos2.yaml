model: "zonos2"
max_seq_len: 8192
# Two-GPU: GPU 0 runs the LLM (prefill + decode), GPU 1 runs the DAC vocoder.
node_groups:
  - node_names: [LLM]
    ranks: [0]
    graph_walks: [prefill, decode]
  - node_names: [dac_decoder]
    ranks: [1]
    graph_walks: [dac_chunk]
