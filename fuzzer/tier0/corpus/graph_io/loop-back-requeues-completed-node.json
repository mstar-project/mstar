{
  "config": {
    "loop": {
      "emit": true,
      "end": 1,
      "max_iters": 1,
      "start": 0
    },
    "widths": [
      2,
      2,
      2,
      2
    ]
  },
  "machine": "graph_io",
  "notes": {
    "diagnosis": "A loop-back edge buffered for the next iteration still re-queues its destination as ready for the current one, because the queue-add tests the current slot.",
    "invariant": "graph.ready_implies_all_inputs",
    "seed": 16,
    "signature": [
      "invariant",
      "graph.ready_implies_all_inputs"
    ],
    "status": "open",
    "summary": "[graph.ready_implies_all_inputs] n0_1 is queued as ready but is missing input(s) ['x0_0_1', 'x0_1_1']; it would run on a partial input set"
  },
  "ops": [
    [
      "drain",
      []
    ]
  ]
}
